# Local development convenience overrides.
# Run with: --spring.profiles.active=dev  (or SPRING_PROFILES_ACTIVE=dev)
# Keeps the old easy defaults for local iteration without making them the
# production fallback in application.properties.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.h2.console.enabled=true